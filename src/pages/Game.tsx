import * as React from 'react';

import styles from './Game.module.scss';

const Game: React.FC = () => {

    return (
        <section>
            game
        </section>
    );
};

export { Game };
